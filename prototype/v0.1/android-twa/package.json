{
  "name": "android-twa",
  "private": true,
  "version": "1.0.0",
  "description": "Envoltorio Trusted Web Activity (Bubblewrap) de la PWA de CapyMeal, para distribuir un .apk instalable sin pasar por Play Store. Ver README.md.",
  "scripts": {
    "setup": "node .scripts/setup-config.js",
    "generate": "node .scripts/generate-project.js",
    "build": "node .scripts/build.js"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@bubblewrap/cli": "^1.25.0",
    "@bubblewrap/core": "^1.25.0"
  }
}
